# JWT Secret Key
# Required: Secret key used for signing and verifying JWT tokens
# Generate a strong random string for production (e.g., using openssl rand -base64 32)
JWT_SECRET=your-secret-key-here

# Server Port
# Optional: Port for the backend server (defaults to 3001)
PORT=3001

# Frontend URL
# Optional: URL of the frontend application for CORS configuration (defaults to http://localhost:3000)
FRONTEND_URL=http://localhost:3000

# Node Environment
# Optional: Set to 'production' for production builds (affects cookie security settings)
NODE_ENV=development

# Backend URL
# Optional: URL of the backend API server (defaults to http://localhost:3001)
# Used by Next.js API proxy routes
BACKEND_URL=http://localhost:3001
